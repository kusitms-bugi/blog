name: Sync Notion Posts

on:
  # 매시간 Notion 체크 (한국 시간 기준 매 정시)
  schedule:
    - cron: '0 * * * *'
  
  # 수동 실행 가능
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 코드
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install 의존성
        run: npm ci
      
      - name: Notion에서 포스트 가져오기
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
        run: npm run cache:posts
      
      - name: 변경사항 확인
        id: check_changes
        run: |
          if git diff --quiet posts-cache.json; then
            echo "변경사항 없음"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "새 포스트 발견!"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
      
      - name: 변경사항 커밋 & 푸시
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add posts-cache.json
          git commit -m "chore: Notion 포스트 자동 동기화 [skip ci]"
          git push
      
      - name: Vercel 재배포 트리거
        if: steps.check_changes.outputs.has_changes == 'true'
        env:
          VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          echo "🚀 Vercel 재배포 트리거 중..."
          curl -X POST "$VERCEL_DEPLOY_HOOK"
          echo "✅ 재배포 완료!"
